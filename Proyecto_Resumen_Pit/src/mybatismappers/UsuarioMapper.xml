<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-/mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UsuarioSQL">
    
    <select id="SQL_listadoUsuarios" resultType="UsuarioMyBatis" parameterType="map">
       
         select * from tb_usuario where
        <choose>
            <when test="_parametro == 'nombre'">
                nombre like #{_valor}
            </when>
            <when test="_parametro == 'apellido'">
                apellido like #{_valor}
            </when>
            <otherwise>
                usuario=usuario
            </otherwise>
        </choose>
        
  
        
      
        
    </select>
    
    
</mapper>